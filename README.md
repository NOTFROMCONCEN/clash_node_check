# Clash Node Checker

一个使用 Rust + egui/eframe 搭建的 Clash 订阅节点存活检测 GUI 骨架。

当前版本支持：

- 输入 Clash 订阅 URL
- 下载订阅内容
- 解析 Clash YAML 中的 `proxies`
- 兼容常见 base64 URI 订阅列表的节点名、服务器和端口提取
- DNS 解析检测（可解析 IP 数）
- TCP 连通检测（多次采样、成功次数、平均耗时）
- TLS ClientHello 预检（可选，按协议自动筛选目标）
- 安全性评估（高/中/低，0~100 综合评分）
- 加密程度评估（强/中/弱/明文/未知）
- 订阅质量检测（重复端点、重名节点、唯一端点统计）
- 结果筛选与搜索（全部/通过/部分/失败/高风险）
- 并发线程数、超时、采样次数可配置
- 在 GUI 中展示通过/部分/失败、TCP 可连率、TLS 通过率与详细状态

## 运行

```powershell
cargo run
```

## 后续可扩展方向

- 按协议实现真实代理握手检测（trojan/vmess/vless 等）
- 增加抖动、超时率、连续失败率等稳定性指标
- 支持从本地 YAML 文件导入
- 导出检测结果 CSV/JSON
- 增加历史对比与回归告警
